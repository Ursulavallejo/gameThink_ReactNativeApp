import { Text, View } from 'react-native'
import { globalStyles } from '../../styles/globalStyles'

export function CatalogScreen() {
  return (
    <View style={globalStyles.layout}>
      <Text style={globalStyles.title}>CATALOG API Games + search</Text>
    </View>
  )
}
